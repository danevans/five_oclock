{
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "countries-and-timezones": "^3.7.2",
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url https://github.com/danevans/five_oclock/",
    "deploy": "gh-pages -d dist"
  }
}
